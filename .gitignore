# --- Secrets (V3 §7.4 — never commit) ---
.env
*.key
*.pem
credentials.json

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# --- Virtual env / uv ---
.venv/
venv/
.repro/

# --- Tooling caches ---
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
coverage.xml

# --- Target repository checkout (cloned at runtime, not versioned) ---
data/target_repo/
data/*.tar.gz

# --- graph.html IS a committed deliverable (Graphify export triad) ---
artifacts/*.tmp
# pyvis local-asset dir (we use cdn_resources="remote", so this shouldn't appear)
lib/

# --- OS ---
.DS_Store
Thumbs.db
