# --- Secrets (V3 §7.4 — never commit) ---
.env
*.key
*.pem
credentials.json

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# --- Virtual env / uv ---
.venv/
venv/

# --- Tooling caches ---
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
coverage.xml

# --- Target repository checkout (cloned at runtime, not versioned) ---
data/target_repo/
data/*.tar.gz

# --- Large generated graph artifacts (regenerated by `graphquest graphify`) ---
artifacts/graph.html
artifacts/*.tmp

# --- OS ---
.DS_Store
Thumbs.db
