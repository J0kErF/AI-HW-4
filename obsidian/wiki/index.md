# Index — Portfolio Entry (Macro view)

> **Template / placeholder.** Regenerated by `VaultWriter` on `graphquest graphify`.
> This is the Obsidian vault's home page: the *Macro* reading of the system —
> structure first, then communities, then specific nodes.

## What this vault is
A navigable map of the target codebase (`data/target_repo`). Open it in
Obsidian; follow `[[wikilinks]]` from system → community → node → source.

## System structure (filled in by Graphify)
- **Domains / layers:** _generated_
- **Communities (connectivity clusters, may cross folders):** _generated_
- **Central nodes / hubs:** _generated_ → see [[hot]]
- **Bridges (cross-community dependencies):** _generated_
- **Isolated clusters (findings, not diagnoses):** _generated_

## Main navigation paths
- Entry points → core services → data/IO
- Failing-test symptom → suspected community → [[hot]]

## Reading discipline
Macro → Meso → Micro. Every edge is a *claim* with `evidence`
(extracted/inferred/ambiguous), `confidence`, and a `source_file`. Validate
INFERRED/AMBIGUOUS edges against source before concluding.
