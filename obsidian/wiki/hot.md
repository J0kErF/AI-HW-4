# Hot — Focused Bug Context (Meso/Micro view)

> **Template / placeholder.** Regenerated by `VaultWriter`. This is the agent's
> cheap entry point: the smallest context that covers the bug-critical region,
> assembled from centrality + the suspected-bug community. It is the single page
> the debugging agent reads first (instead of raw files).

## Failing symptom
_The failing test / error to investigate (filled from `setup.json` target)._

## Suspect region (centrality-ranked)
| Node | type | centrality | community | why suspect |
|------|------|-----------|-----------|-------------|
| _generated_ | | | | |

## Critical edges to follow
_`calls` / `tested_by` / `rationale_for` edges around the suspect, with
evidence type + confidence + `source_file`._

## Open questions for the agent
1. Which node's behavior contradicts its `rationale_for` / docstring?
2. Where does an EXTRACTED call chain reach the failing assertion?
3. Which single `source_file` span must be opened to confirm (SRC step)?

> Conclusion language must match evidence strength — "the graph suggests…"
> until validated against `source_file`.
