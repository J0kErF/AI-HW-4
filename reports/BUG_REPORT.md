# Bug Analysis Report

> Filled during Phase 4. Follows the responsible-inference pipeline.

## 1. Problem description
_The failing test / observed symptom._

## 2. Investigation (OBS → REL → CONF → CTX → SRC)
- **OBS** — what we saw on the graph (central node / isolated cluster / missing path).
- **REL** — edges followed (label, direction).
- **CONF** — evidence weighting (EXTRACTED vs INFERRED).
- **CTX** — layer/owner/community/tests/rationale.
- **SRC** — `source_file` opened to confirm.

## 3. Root cause
_File::function and the precise defect._

## 4. Fix
_What changed, why, and how it was verified (target test buggy→fixed)._

## 5. Before / after
_Graph / architecture diff after the fix._
